file_names=$(./scripts/diff-file-names | grep 'src/.*\.\(js\|vue\)$' | xargs) \
  && echo Files: $file_names | tr ' ' '\n' \
  && if [ ! -z "$file_names" ]; then eslint --ext .js,.vue --max-warnings=0 $file_names; fi \
